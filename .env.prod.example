# =============================================================================
# Photo-Processor Production Environment Variables
# Domain: photo.wangdake.de
# =============================================================================
#
# IMPORTANT: Copy this file to .env.prod and fill in ALL required values!
#            cp .env.prod.example .env.prod
#            chmod 600 .env.prod
#
# =============================================================================

# -----------------------------------------------------------------------------
# JWT Authentication (REQUIRED - MUST change in production!)
# -----------------------------------------------------------------------------
# Generate a secure secret with:
#   openssl rand -base64 32
#
# WARNING: Using the default value is a CRITICAL security vulnerability!
# The application will refuse to start if this is not properly configured.
# -----------------------------------------------------------------------------
JWT_SECRET=

# -----------------------------------------------------------------------------
# Admin Account (Created on first startup)
# -----------------------------------------------------------------------------
# Use a strong password in production!
# Minimum 12 characters, mix of letters, numbers, and symbols recommended.
# -----------------------------------------------------------------------------
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

# -----------------------------------------------------------------------------
# Dropbox OAuth (REQUIRED)
# -----------------------------------------------------------------------------
# Create your app at: https://www.dropbox.com/developers/apps
#
# Settings for your Dropbox App:
# - App name: PhotoProcessor (or your preferred name)
# - Permission type: Full Dropbox (or App folder if preferred)
# - OAuth2 redirect URI: https://photo.wangdake.de/api/dropbox/callback
#
# Required permissions:
# - files.metadata.read
# - files.content.write
# - files.content.read
# -----------------------------------------------------------------------------
DROPBOX_APP_KEY=

# -----------------------------------------------------------------------------
# URLs (Pre-configured for photo.wangdake.de)
# -----------------------------------------------------------------------------
# Only change these if using a different domain or port!
# Make sure to update Caddyfile and Dropbox app settings if you change these.
# -----------------------------------------------------------------------------
DROPBOX_REDIRECT_URI=https://photo.wangdake.de/api/dropbox/callback
FRONTEND_URL=https://photo.wangdake.de
CORS_ORIGIN=https://photo.wangdake.de
