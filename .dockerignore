# =============================================================================
# Docker Build Ignore File
# Excludes unnecessary files from Docker build context
# =============================================================================

# Dependencies (will be installed fresh in container)
node_modules
**/node_modules

# Build outputs (will be built in container)
dist
**/dist
.next
**/out
**/build

# Version control
.git
.gitignore
.gitattributes

# Documentation (not needed in production images)
*.md
!README.md
docs/

# Development files
.vscode
.idea
*.swp
*.swo

# Environment files (secrets should be injected at runtime)
.env
.env.*
!.env.prod.example

# Testing
**/__tests__
**/*.test.ts
**/*.spec.ts
coverage/
test-results/
playwright-report/
blob-report/

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*

# OS files
.DS_Store
Thumbs.db

# Temporary files
tmp/
temp/
.cache/

# Turbo cache
.turbo/

# Data directory (runtime data, not for build)
data/
apps/server/data/
*.db
*.sqlite
*.sqlite3

# SSH keys (NEVER include in images!)
*.pem
id_rsa*
id_ed25519*
deploy_key*

# Python project (separate from this build)
photo-download/

# Claude/AI context files
.claude/
CLAUDE.md

# GitHub (not needed in container)
.github/

# Scripts that are not needed in container build
# (they are for deployment, not runtime)
# deploy.sh is copied separately in CI/CD
